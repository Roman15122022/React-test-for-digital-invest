import React from 'react';
import '../styles/forTitle.scss'
const Title = ({name}) => {
    return (
        <div className='title'>
            {name}
        </div>
    );
};

export default Title;
